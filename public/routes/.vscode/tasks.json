{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Reload Firebase Local Server",
            "type": "shell",
            "command": "firebase serve",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}